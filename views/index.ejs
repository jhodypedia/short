<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>ShortLink • Pansa</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="page page-home">
<div class="bg-gradient"></div>

<header class="site-header">
    <div class="logo">Short<span>Link</span></div>
</header>

<main class="container">
    <section class="card glass card-main">
        <h1 class="title">Buat Link Pendek</h1>
        <p class="subtitle">Pendek, rapi, dan siap di-share ke mana saja.</p>

        <% if (error) { %>
            <div class="alert alert-error">
                <%= error %>
            </div>
        <% } %>

        <form action="/shorten" method="post" class="short-form">
            <div class="form-group">
                <label for="url">URL Asli</label>
                <input type="text" id="url" name="url" placeholder="https://contoh.com/artikel-super-panjang" required>
            </div>
            <button type="submit" class="btn btn-primary btn-animate">
                <span>Buat Link</span>
            </button>
        </form>

        <% if (shortUrl) { %>
            <div class="result">
                <h2>Link Pendek Kamu</h2>
                <div class="result-row">
                    <input type="text" id="shortUrl" value="<%= shortUrl %>" readonly>
                    <button type="button" class="btn btn-secondary" id="copyBtn">Salin</button>
                </div>
                <p class="hint">Bagikan link ini. Pengunjung akan melihat halaman iklan dulu sebelum diarahkan ke URL asli.</p>
            </div>
        <% } %>
    </section>

    <footer class="site-footer">
        <p>Made with ⚡ by Pansa</p>
    </footer>
</main>

<script src="https://code.jquery.com/jquery-3.7.1.min.js"
        integrity="sha256-3fp9G6bT5N2z3N4R0rT7xvFeoJq6Digw1k3jv4mxYyk="
        crossorigin="anonymous"></script>
<script>
    $(function () {
        $('#copyBtn').on('click', function () {
            const input = document.getElementById('shortUrl');
            if (!input) return;

            input.select();
            input.setSelectionRange(0, 99999);
            document.execCommand('copy');

            $(this).text('Tersalin!');

            setTimeout(() => {
                $('#copyBtn').text('Salin');
            }, 1500);
        });
    });
</script>
</body>
</html>
